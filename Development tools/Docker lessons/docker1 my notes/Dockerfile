# Докер используется для установки проекта на сервер или на лок. машину.
# Докер - запуск чего угодно где угодно

# image - образ 
# container - контейнер
# volume - дисковые хранилища. Не очищаются после работы докера

# Докер файл ниже

# Установка python облечённой версии
FROM python:3.8-slim-buster
# Папка где будет работать приложение
WORKDIR /app
# Копирование файла requirements(содержит названия нужных библиотек для запуска нашего приложения) в докер образ
COPY requirements requirements
# Установка библиотек из файла requirements
RUN pip3 install -r requirements
# Коппирование всех остальных файлов в образ докер
COPY . .
# Команда для запуска нашего приложения
CMD [ "python3", "main.py"]

# Команды нужно вводить в терминале, где находится докер файл

# Команда example-image - название образа :top - тег (исп. для указания версии, сортировки и т.д.)
# docker build -t example-image:top . 

# Команда для создания контейнера из образа
# docker run --name example-app example-image:top 

# Просмотр текущих контейнеров, чтобы узнать их id
# docker ps --all

# Команда для остановки контейнера
# docker stop (id контейнера)

# Команда для удаления контейнера
# docker rm   (id контейнера)

# У Pycahrm есть свой генератор докер файлов
