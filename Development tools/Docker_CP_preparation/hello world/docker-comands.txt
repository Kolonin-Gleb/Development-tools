# Команды нужно вводить в терминале, где находится докер файл.

# Создание образа
# docker build -t название_образа_докера откуда_собирать_образ
# docker build -t hello_world_image .

# Просмотр всех образов
# docker images

# Создание контейнера из образа
# docker run --name hello_world_app_container hello_world_image 

# Просмотр активных контейнеров, чтобы узнать их id
# docker ps --all


# Команда для остановки контейнера
# docker stop (id контейнера)

# Команда для удаления контейнера
# docker rm   (id контейнера)


# Просмотр текущих контейнеров, чтобы узнать их id
# docker ps --all

# Команда для остановки контейнера
# docker stop (id контейнера)

# Команда для удаления контейнера
# docker rm (id контейнера)